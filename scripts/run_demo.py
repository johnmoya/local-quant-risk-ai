"""End-to-end demo script: load a sample return series, compute VaR/ES via
all three methods, run a backtest, and (once M7 lands) fetch an LLM
explanation of the results.

Implemented incrementally, module by module, starting M2.
"""
