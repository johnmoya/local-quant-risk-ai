"""Reproducible research scripts built on top of the risk engine.

Nothing here is part of the shipped package: `src/quant_risk_ai` never
imports this directory, and its dependencies live in the `research` extra
rather than in the runtime requirements. These modules only orchestrate
functions the engine already exposes — no risk formula is reimplemented.
"""
