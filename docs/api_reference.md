# API Reference

Populated in M6 (risk endpoints: `/var`, `/expected-shortfall`, `/backtest`)
and M7 (`/explain`). FastAPI's auto-generated OpenAPI docs
(`/docs`, `/redoc`) will be the live reference once the service exists;
this file adds the narrative context OpenAPI doesn't capture (e.g. the
`/explain` endpoint's failure-isolation behavior when Ollama is
unavailable).
